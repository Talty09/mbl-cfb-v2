# Copy to .dev.vars for local `wrangler dev`. In production these are set with
# `wrangler secret put <NAME>` and are never committed.

# College Football Data API key — https://collegefootballdata.com/key
# Used only by the ingest cron; the client never sees it and there is no
# passthrough proxy.
CFBD_API_KEY=
